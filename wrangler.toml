name = "scout-firenze1"
main = "workers/scout-api.js"
compatibility_date = "2026-09-08"

[assets]
directory = "."
binding = "ASSETS"
run_worker_first = ["/api/*"]

# Binding KV: crealo dalla Dashboard Cloudflare (non serve metterlo qui).
# Workers & Pages → scout-firenze1 → Settings → Bindings → Add → KV Namespace
# Variable name: SCOUT_KV  |  Namespace: crea "firenze1-sync"
